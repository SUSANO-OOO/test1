import { chromium } from 'playwright';
import fs from 'node:fs/promises';
import path from 'node:path';
const baseUrl=process.env.BASE_URL||'http://127.0.0.1:4173/index.html';
const outputDir=path.resolve('qa-artifacts');
const viewports=[{name:'desktop-1440',width:1440,height:900},{name:'tablet-1024',width:1024,height:768},{name:'mobile-390',width:390,height:844}];
await fs.mkdir(outputDir,{recursive:true});const report={baseUrl,startedAt:new Date().toISOString(),viewports:[],failures:[]};
for(const viewport of viewports){const browser=await chromium.launch({headless:true});const context=await browser.newContext({viewport:{width:viewport.width,height:viewport.height},deviceScaleFactor:1});const page=await context.newPage();const result={...viewport};try{const started=Date.now();await page.goto(baseUrl,{waitUntil:'domcontentloaded',timeout:60000});await page.waitForSelector('.hero h1');await page.waitForFunction(()=>{const i=document.querySelector('.portrait-wrap img');return i?.complete&&i.naturalWidth>0},{timeout:30000});result.readyMs=Date.now()-started;result.runtime=await page.evaluate(()=>{const resources=performance.getEntriesByType('resource');const scripts=[...document.scripts].map(s=>s.src).filter(Boolean);const styles=[...document.querySelectorAll('link[rel="stylesheet"]')].map(l=>l.href);return{scripts,styles,externalScripts:scripts.filter(s=>new URL(s,location.href).origin!==location.origin),resourceCount:resources.length,transferBytes:resources.reduce((n,r)=>n+(r.transferSize||0),0),encodedBytes:resources.reduce((n,r)=>n+(r.encodedBodySize||0),0),canvasCount:document.querySelectorAll('canvas').length};});result.framePacing=await page.evaluate(()=>new Promise(resolve=>{const gaps=[];let prev=performance.now();let start=prev;function tick(now){gaps.push(now-prev);prev=now;if(now-start>1200){const sorted=[...gaps].sort((a,b)=>a-b);resolve({p95:sorted[Math.floor(sorted.length*.95)]||0,max:Math.max(...gaps),count:gaps.length});return}requestAnimationFrame(tick)}requestAnimationFrame(tick)}));if(result.readyMs>2500)throw new Error(`${viewport.name}: ready ${result.readyMs}ms`);if(result.runtime.externalScripts.length)throw new Error(`${viewport.name}: external scripts remain`);if(result.runtime.canvasCount!==0)throw new Error(`${viewport.name}: unnecessary canvas remains`);if(result.framePacing.p95>120)throw new Error(`${viewport.name}: frame pacing p95 ${result.framePacing.p95}`);if(result.framePacing.max>220)throw new Error(`${viewport.name}: frame stall ${result.framePacing.max}`);}catch(error){const failure=String(error?.stack||error);result.failure=failure;report.failures.push({viewport:viewport.name,failure});}finally{report.viewports.push(result);await browser.close()}}
report.finishedAt=new Date().toISOString();await fs.writeFile(path.join(outputDir,'performance-report.json'),JSON.stringify(report,null,2),'utf8');console.log(JSON.stringify(report,null,2));if(report.failures.length)process.exit(1);